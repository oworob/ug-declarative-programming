; Please define a destructive version append! of append. What are the values of the following expressions?
; a) (define x '(a b)) 
; b) (define y '(c d)) 
; c) (append x y) 
; d) (cdr x) 
; e) (append! x y) 
; f) (mcdr x) 

; ??????